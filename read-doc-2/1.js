let iceCreamTaste = 'chocolate'; // change variable to camelCase, add semicolon
let iceCreamDensity = 10; // change variable to camelCase, add semicolon

while (iceCreamDensity > 0) { // add space, move curly bracket up a line
    console.log('Drip...');
    iceCreamDensity -= 1;
}

console.log('The ' + iceCreamTaste + ' ice cream melted.'); // add spaces
